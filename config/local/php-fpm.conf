[global]
pid = run/php-fpm.pid
error_log = logs/php-fpm.log
log_level = warning

[www]
user = nobody
group = nobody
listen = /run/php/php-fpm.sock
listen.owner = nobody
listen.group = nobody
pm = dynamic
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 5
